# MySQL Client Configuration
# Disables SSL to avoid certificate verification errors when loading/dumping schemas
# Applies to all MySQL client tools (mysql, mysqldump, etc.)

[client]
skip-ssl

[mysql]
skip-ssl

[mysqldump]
skip-ssl
